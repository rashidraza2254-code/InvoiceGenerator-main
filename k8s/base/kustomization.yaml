apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base manifests — referenced by overlays/production/
resources:
  - namespace.yaml
  - configmap.yaml
  - mongodb-deployment.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - ingress.yaml
